# BotForge Ping-Pong Bot (Telegram Sample)

A minimal sample showing how to wire BotForge to Telegram and respond with a simple ping/pong interaction.

## Run
1. Set your bot token in configuration (e.g., `appsettings.json` -> `ApiKeys:Telegram`) or pass it directly.
2. Run the project.
