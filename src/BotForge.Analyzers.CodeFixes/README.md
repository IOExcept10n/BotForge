# BotForge.Analyzers.CodeFixes

Code fixes accompanying the BotForge analyzers. This project provides automatic fixes for diagnostics raised by BotForge.Analyzers.

You usually do not reference this project directly. Instead, install the meta-package:
- `dotnet add package BotForge.Analyzers`

That package includes both analyzers and code fixes for consumer projects.
